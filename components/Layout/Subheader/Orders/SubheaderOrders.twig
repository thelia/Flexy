<div class='Subheader Subheader--orders'>
  <div class='container mx-auto flex flex-col items-center'>
    <h1 class='Subheader-title'>{{ title }}</h1>
  <div class='flex'>
    {% include '../../../Molecules/Button/Button.twig' with {text: 'En cours', sharp:true, variant:selected == 'in_progress' ? 'primary' : 'secondary'} %}
    {% include '../../../Molecules/Button/Button.twig' with {text: 'Historique', sharp:true, variant: selected == 'in_progress' ? 'secondary' : 'primary'} %}
  </div>
  </div>
</div>
